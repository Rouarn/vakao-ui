/**
 * 图标配置模块
 *
 * 提供脚本系统中使用的 Emoji 图标配置，用于美化终端输出。
 * 为不同类型的操作和状态提供直观的视觉标识。
 *
 * 主要特性：
 * - 语义化图标：每个图标都有明确的含义
 * - 统一风格：保持图标风格的一致性
 * - 易于扩展：可以轻松添加新的图标类型
 * - 跨平台兼容：使用标准 Unicode Emoji
 *
 * @example
 * ```javascript
 * const { icons } = require('./icons');
 *
 * // 在日志中使用图标
 * console.log(`${icons.success} 操作成功完成`);
 * console.log(`${icons.error} 操作失败`);
 * console.log(`${icons.build} 正在构建项目...`);
 * ```
 *
 * @version 1.0.0
 * @author Vakao UI Team
 */

/**
 * 图标映射配置
 *
 * 为不同的操作类型和状态定义对应的 Emoji 图标。
 * 图标选择遵循直观性和通用性原则。
 */
const icons = {
  /** 信息提示图标 - 用于一般信息输出 */
  info: "📝",

  /** 成功状态图标 - 用于操作成功提示 */
  success: "✅",

  /** 警告状态图标 - 用于警告信息提示 */
  warning: "⚠️",

  /** 错误状态图标 - 用于错误信息提示 */
  error: "❌",

  /** 命令执行图标 - 用于命令行操作提示 */
  command: "🔧",

  /** 构建操作图标 - 用于项目构建过程 */
  build: "🏗️",

  /** 发布操作图标 - 用于包发布过程 */
  publish: "📦",

  /** 部署操作图标 - 用于项目部署过程 */
  deploy: "🚀",

  /** 检查操作图标 - 用于检查和验证过程 */
  check: "🔍",

  /** 复制操作图标 - 用于文件复制操作 */
  copy: "📋",

  /** 清理操作图标 - 用于清理和删除操作 */
  clean: "🧹",
};

export { icons };
